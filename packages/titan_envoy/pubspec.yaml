name: titan_envoy
description: "Envoy — Titan's HTTP client & API layer. Interceptors, caching, auth, metrics, and route-based data loading."
version: 1.1.1
homepage: https://ikolvi.com
repository: https://github.com/Ikolvi/titan
issue_tracker: https://github.com/Ikolvi/titan/issues

topics:
  - http
  - api-client
  - networking
  - dart
  - interceptors

screenshots:
  - description: 'Envoy — HTTP client & API layer for Titan'
    path: assets/titan_banner.webp

resolution: workspace

environment:
  sdk: ^3.10.3

dependencies:
  titan: ^1.1.3
  titan_basalt: ^1.12.6
  meta: ^1.16.0

dev_dependencies:
  test: ^1.25.8
  lints: ^6.0.0
